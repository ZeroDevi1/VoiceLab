[project]
name = "voicelab-gpt-sovits"
version = "0.1.0"
description = "VoiceLab workflow: GPT-SoVITS (skeleton)"
requires-python = ">=3.10,<3.11"
dependencies = [
  "PyYAML>=6.0.2",
]

[tool.uv]
package = false

