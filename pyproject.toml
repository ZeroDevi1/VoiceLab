[project]
name = "voicelab"
version = "0.1.0"
description = "VoiceLab workspace bootstrap (vendor sync + per-workflow uv envs)"
requires-python = ">=3.10"
dependencies = [
    "packaging>=26.0",
    "setuptools>=82.0.0",
    "wheel>=0.46.3",
]

[tool.uv]
package = false

